#include<stdio.h>
#include<stlib.h>

int struct Polynode{
    int coeff;
    int expo;
    struct Polynode* next;
};

Polynode create_node(int coeff, int expo){
Polynode* new_node = (Polynode*)malloc(sizeof(polynode))

}
int main()
{
    
    return 0;
}